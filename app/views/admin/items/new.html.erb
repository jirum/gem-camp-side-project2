<div class="main">
  <div class="card w-50 bg-light" style="margin: 0 auto; float: none; margin-top: 70px;">
    <div class="card-body">
      <%= render 'errors/error', model: @item %>
      <%= form_for @item, url: admin_items_path, method: :post do |f| %>
        <%= render partial: 'form', locals: {f: f, item: @item} %>
      <% end %>
    </div>
  </div>
</div>
